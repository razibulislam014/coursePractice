export default function Avatar({ person, size }) {
  //   const { person, size } = props;

  return (
    <div>
      <div>
        <h1>{person}</h1>
        <p>{size}</p>
      </div>
    </div>
  );
}
